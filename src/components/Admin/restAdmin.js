const { ReactComponent } = require("*.svg");
import React, { Component } from "React";
import Context from "../../Context";

export default class RestAdmin extends Component {
  render() {
    return o;
  }
}
